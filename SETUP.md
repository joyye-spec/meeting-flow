# 会议纪要自动化 · 配置指南

基于 openclaw + 飞书多维表格 + 豆包 ASR 的完整会议纪要自动化方案。

录音上传 → 自动转写 → AI 生成纪要 → 回填飞书文档 + 多维表格。

> **使用方式**：把这份文档给 Claude（或其他 AI agent），它会引导你完成所有配置步骤。你也可以按步骤自行操作。标注「你来做」的步骤需要手动操作，标注「自动完成」的步骤由 Claude 代劳。

---

## 配置流程（共 12 步，约 15 分钟）

---

### 步骤 1：创建飞书机器人应用（你来做，约 5 分钟）

1. 打开 https://open.feishu.cn/app ，点击「创建企业自建应用」
2. 填写应用名称（任意），应用类型选「机器人」，创建
3. 进入「权限管理」，搜索并开通以下权限：
   - `im:message`（发送和接收消息）
   - `bitable:app`（读写多维表格）
   - `drive:file`（获取文件下载链接）
4. 进入「版本管理与发布」，创建版本并发布
5. 回到「凭证与基础信息」页面，记下：
   - **App ID**（`cli_` 开头）
   - **App Secret**

---

### 步骤 2：获取你的 open_id（你来做）

你的飞书 `open_id`（`ou_` 开头）是后续配置必填项。有三种方式获取：

**方式 A — 通过邮箱查询（有 lark-cli 时推荐）**

```bash
lark-cli api GET "/open-apis/contact/v3/users/by_email?emails=你的飞书邮箱" --as bot
```

返回 JSON 中的 `open_id` 字段即为所需值。

**方式 B — 通过 Bot 事件获取（最通用）**

1. 在飞书开放平台进入你创建的应用 → 「事件订阅」→「调试工具」
2. 监听 `im.message.receive_v1` 事件
3. 给 Bot 发一条私聊消息
4. 在事件日志中找到 `sender.sender_id.open_id`

**方式 C — 通过开放平台调试台**

打开 https://open.feishu.cn ，使用 API 调试台，搜索「通过手机号或邮箱获取用户ID」，填入信息即可。

> ⚠️ 飞书客户端「我的资料」页面**不会显示** open_id，必须通过以上 API 方式获取。

---

### 步骤 3：开通火山引擎 ASR（你来做，约 5 分钟）

1. 打开 https://www.volcengine.com/ 注册/登录账号
2. 搜索「录音文件识别」，或进入「语音技术」→「录音文件识别（大模型版）」
3. 点击「立即开通」
4. 开通后进入控制台，记下：
   - **AppID**（纯数字）
   - **Access Token**（长字符串）

集群默认用 `volc_auc_common` 即可。

---

### 步骤 4：选择纪要生成方式（你来做）

**方案 A（推荐）**：直接用 Claude 生成，无需额外安装，现在就能用。

**方案 B**：用 Codex CLI 生成，在独立沙箱里运行，适合对隔离性有要求的场景，但需要先安装 Node.js 和 Codex（见步骤 5）。

---

### 步骤 5：安装 Codex CLI（方案 B 才需要，你来做）

```bash
# 如果已有 Node.js v18+，直接安装：
npm install -g @openai/codex

# 推荐用 nvm 管理版本：
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 22 && nvm use 22
npm install -g @openai/codex
```

安装完成后执行：
```bash
which codex
```

记下输出的路径（如 `/Users/xxx/.nvm/versions/node/v22.x.x/bin/codex`）。如果需要代理访问 OpenAI，也记下代理端口（如 `7890`）。

方案 A 跳过此步骤，直接进入步骤 6。

---

### 步骤 6：创建飞书多维表格（自动完成）

Claude 会使用 lark-base skill 按以下字段结构创建多维表格：

| 字段名 | 字段类型 | 备注 |
|--------|---------|------|
| 会议名称 | 文本 | — |
| 录音文件 | 附件 | — |
| 纪要模板类型 | 单选 | 选项：商务版纪要、详细版纪要、行动项版纪要 |
| 状态 | 单选 | 选项：处理中、失败、已完成 |
| 转写文字链接 | 超链接 | — |
| 会议纪要链接 | 超链接 | — |
| 创建时间 | 创建时间 | — |

完成后会得到：
- `app_token`（多维表格 URL 中的唯一标识）
- `table_id`（`tbl` 开头）

---

### 步骤 7：创建表单视图（自动完成）

在多维表格里创建表单视图，开放字段：会议名称、录音文件、纪要模板类型。

完成后会得到：
- `view_id`（`vew` 开头）
- 表单完整链接（`YOUR_FORM_URL`）

---

### 步骤 8：配置多维表格自动化（你来做，约 3 分钟）

这是整条链路里唯一必须手动操作的步骤——飞书多维表格自动化目前没有开放配置 API。

1. 打开多维表格（Claude 会把链接发给你）
2. 点击右上角「自动化」→「新建自动化」
3. **触发条件**：选「记录满足条件时」→ 条件设为「录音文件」「不为空」
4. **执行动作**：选「发送飞书消息」→ 接收人选你的工作群
5. **消息内容**（必须包含以下关键词，Bot 靠这个识别触发）：

   ```
   录音文件更新提醒
   会议名称：{{会议名称}}
   记录链接：{{当前记录链接}}
   <at id=ou_xxxxx></at>
   ```

   **关键步骤：@Bot 必须通过输入框的 @ 选择器操作**，不能手打 `@Bot名`：
   - 在消息编辑器里输入 `@`，搜索你的 Bot 名称，从下拉列表中选中
   - 这样会生成 `<at id=ou_xxxxx></at>` 格式的标记，这才是 Bot 收到事件的关键
   - 飞书可能有多个同名 Bot，务必确认选的是你自己创建的那个（对应你的 App ID）

6. 保存并启用

> ⚠️ 如果以后修改了消息模板中的「录音文件更新提醒」这段文字，需同步修改 SKILL.md 阶段二的触发匹配关键词，否则 Bot 会失效。

---

### 步骤 9：获取工作群 chat_id（自动完成）

```bash
lark-cli chat list
# 找到工作群名称对应的 chat_id（oc_ 开头）
```

---

### 步骤 10：配置 openclaw 群聊（自动完成）

编辑 openclaw.json，添加群聊配置：

```json
{
  "groups": {
    "YOUR_GROUP_CHAT_ID": {
      "requireMention": false
    }
  }
}
```

> ⚠️ 不能用 `gateway config.patch` 修改这个字段，必须直接编辑文件后重启。

---

### 步骤 11：替换 SKILL.md 中的所有占位符（自动完成）

用前面收集到的值替换 SKILL.md 配置区的所有 `YOUR_XXX`：

| 占位符 | 来源 | 备注 |
|--------|------|------|
| `YOUR_USER_OPEN_ID` | 步骤 2 | — |
| `YOUR_GROUP_CHAT_ID` | 步骤 9 | — |
| `YOUR_BITABLE_APP_TOKEN` | 步骤 6 | — |
| `YOUR_TABLE_ID` | 步骤 6 | — |
| `YOUR_FORM_VIEW_ID` | 步骤 7 | — |
| `YOUR_FORM_URL` | 步骤 7 | — |
| `YOUR_VOLCENGINE_APP_ID` | 步骤 3 | — |
| `YOUR_VOLCENGINE_TOKEN` | 步骤 3 | — |
| `YOUR_CODEX_PATH` | 步骤 5 | 仅方案 B，否则删除该行 |
| `YOUR_PROXY_PORT` | 步骤 5 | 仅方案 B 且需要代理，否则删除相关两行 |
| `YOUR_OBSIDIAN_PATH` | 询问用户 | 不用 Obsidian 则删除该行 |

同时，在 SKILL.md 阶段四中：
- 方案 A：删除「方案 B」整块内容
- 方案 B：删除「方案 A」整块内容，并将 `YOUR_CODEX_PATH` 和 `YOUR_PROXY_PORT` 替换为实际值

---

### 步骤 12：验证整条链路（你配合，Claude 监控）

1. 私聊 Bot 发「记会议纪要」
2. 点击卡片填写表单，上传一个很短的录音文件（30秒以内即可）
3. 提交后等待，Claude 会监控整个处理过程

逐步确认：
- [ ] 私聊卡片正常弹出
- [ ] 工作群收到「录音文件更新提醒」消息并 @了 Bot
- [ ] bitable 状态变为「处理中」
- [ ] ASR 转写完成
- [ ] 纪要生成并回填 bitable 链接
- [ ] bitable 状态变为「已完成」

全部通过 → 配置完成，可以正式使用。

---

## 组件选择说明

| 组件 | 用途 | 是否必须 |
|------|------|---------|
| openclaw + 飞书机器人 | Bot 收发消息、群聊事件驱动 | ✅ 必须 |
| 飞书多维表格 + 表单 + 自动化 | 录音收集 + 流程触发 | ✅ 必须 |
| 豆包 ASR（火山引擎） | 录音转写 + 说话人分离 | ✅ 必须 |
| Codex CLI | 用独立沙箱生成纪要 | ⬜ 可选，不装则 Claude 直接生成 |
| Obsidian | 本地备份会议纪要 | ⬜ 可选 |

---

## 多维表格字段结构（参考）

| 字段名 | 字段类型 | 说明 |
|--------|---------|------|
| 创建时间 | 创建时间 | 自动填充 |
| 会议名称 | 文本 | 用户填写 |
| 录音文件 | 附件 | 用户上传 |
| 纪要模板类型 | 单选 | 商务版纪要 / 详细版纪要 / 行动项版纪要 |
| 状态 | 单选 | 处理中 / 失败 / 已完成（空=待处理） |
| 转写文字链接 | 超链接 | Bot 回填 |
| 会议纪要链接 | 超链接 | Bot 回填 |
| 标签 | 文本 | 可选 |

---

## 常见问题

**Bot 收不到群消息**

1. **Bot 是否在群里？** → 在群成员列表中确认 Bot 存在，不在则先拉入
2. **是否 @了 Bot？** → 多维表格自动化消息里必须通过 @ 选择器选中 Bot（生成 `<at id=ou_xxx></at>` 标记），手打 `@Bot名` 不生效
3. **requireMention 配置是否生效？** → 必须直接编辑 openclaw.json 后重启，不能用 `gateway config.patch`

**群消息发出了但 Bot 没反应**
→ 检查自动化消息里是否包含「录音文件更新提醒」关键词

**ASR 一直轮询 code=2000**
→ 2000 是「正在转写中」不是错误，继续等待，不要停止轮询

**ASR 没有说话人标记**
→ 参数名必须是 `with_speaker_info`，放在 `additions` 里，其他写法被静默忽略

**飞书文档只写入了一小段**
→ 长文本改用 block API 分块写入（5500字/块），见 SKILL.md 阶段五

**Codex hang 住无响应**
→ `pkill -f codex` 后重试，旧 session 未退出会阻塞后续所有调用

**bitable 回填失败 URLFieldPropertyError**
→ 省略 property 参数；值格式必须是 `{"link": "...", "text": "..."}`
