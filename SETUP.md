# 会议纪要自动化 · 配置指南

基于 openclaw + 飞书多维表格 + 豆包 ASR 的完整会议纪要自动化方案。

录音上传 → 自动转写 → AI 生成纪要 → 回填飞书文档 + 多维表格。

---

## Agent 引导配置流程

> **本节供 agent 执行**。首次配置时按步骤走，每步标注了谁来做、做什么、怎么验证。

---

### 步骤 1：收集飞书机器人凭证（需用户操作）

**向用户发送以下指引，等待用户返回结果：**

---

> 我需要你在飞书开放平台创建一个机器人应用，全程大约 5 分钟。
>
> 1. 打开 https://open.feishu.cn/app ，点击「创建企业自建应用」
> 2. 填写应用名称（任意），应用类型选「机器人」，创建
> 3. 进入「权限管理」，搜索并开通以下权限：
>    - `im:message`（发送和接收消息）
>    - `bitable:app`（读写多维表格）
>    - `drive:file`（获取文件下载链接）
> 4. 进入「版本管理与发布」，创建版本并发布
> 5. 回到「凭证与基础信息」页面，把以下两项发给我：
>    - **App ID**（`cli_` 开头）
>    - **App Secret**

---

**收到用户返回后**：记录 `FEISHU_APP_ID` 和 `FEISHU_APP_SECRET`，继续步骤 2。

---

### 步骤 2：获取用户 open_id（agent 自动执行）

拿到 App ID 和 App Secret 后，用 lark-cli 查询用户 open_id：

```bash
lark-cli user get --email 用户邮箱
# 或
lark-cli user get --phone 用户手机号
```

返回结果里的 `open_id` 字段（`ou_` 开头）即为 `YOUR_USER_OPEN_ID`。

---

### 步骤 3：收集 ASR 凭证（需用户操作）

**向用户发送以下指引，等待用户返回结果：**

---

> 我需要你在火山引擎开通录音转写服务，大约 5 分钟。
>
> 1. 打开 https://www.volcengine.com/ 注册/登录账号
> 2. 搜索「录音文件识别」，或直接进入「语音技术」→「录音文件识别（大模型版）」
> 3. 点击「立即开通」
> 4. 开通后进入控制台，找到以下两项发给我：
>    - **AppID**（纯数字）
>    - **Access Token**（长字符串）
>
> 集群不用管，默认用 `volc_auc_common` 即可。

---

**收到用户返回后**：记录 `VOLCENGINE_APP_ID` 和 `VOLCENGINE_TOKEN`，继续步骤 4。

---

### 步骤 4：确认生成纪要的方式（询问用户）

**向用户发送：**

---

> 生成会议纪要有两种方式，请选择：
>
> **方案 A（推荐）**：直接用我来生成，无需额外安装任何东西，现在就能用。
>
> **方案 B**：用 Codex CLI 生成，在独立沙箱里运行，适合对隔离性有要求的场景，但需要先安装 Node.js 和 Codex。
>
> 回复 A 或 B 即可。

---

**如果用户选 A**：记录 `CODEX_MODE=false`，跳过步骤 5，直接到步骤 6。

**如果用户选 B**：进入步骤 5 安装 Codex。

---

### 步骤 5：安装 Codex CLI（方案 B 才需要，需用户操作）

**向用户发送以下指引，等待用户确认完成：**

---

> 需要先安装 Codex CLI。请在终端执行以下命令：
>
> ```bash
> # 如果已有 Node.js v18+，直接安装：
> npm install -g @openai/codex
>
> # 推荐用 nvm 管理版本：
> curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
> nvm install 22 && nvm use 22
> npm install -g @openai/codex
> ```
>
> 安装完成后，执行以下命令，把输出结果发给我：
> ```bash
> which codex
> ```
>
> 另外，如果你需要代理才能访问 OpenAI，把代理端口也告诉我（例如：7890）。

---

**收到用户返回后**：记录 `CODEX_PATH`（如 `/Users/xxx/.nvm/versions/node/v22.x.x/bin/codex`）和 `PROXY_PORT`（没有则为空），继续步骤 6。

---

### 步骤 6：创建飞书多维表格（agent 自动执行）

使用 lark-base skill 创建多维表格，按以下字段结构建表：

| 字段名 | 字段类型 | 备注 |
|--------|---------|------|
| 会议名称 | 文本 | — |
| 录音文件 | 附件 | — |
| 纪要模板类型 | 单选 | 选项：商务版纪要、详细版纪要、行动项版纪要 |
| 状态 | 单选 | 选项：处理中、失败、已完成 |
| 转写文字链接 | 超链接 | — |
| 会议纪要链接 | 超链接 | — |
| 创建时间 | 创建时间 | — |

建表完成后记录：
- `app_token`（多维表格 URL 中的唯一标识）
- `table_id`（`tbl` 开头）

---

### 步骤 7：创建表单视图（agent 自动执行）

在刚创建的多维表格里创建表单视图，开放字段：会议名称、录音文件、纪要模板类型。

记录：
- `view_id`（`vew` 开头）
- 表单完整链接（`YOUR_FORM_URL`）

---

### 步骤 8：配置多维表格自动化（需用户操作，约 3 分钟）

**向用户发送以下指引（附上多维表格链接），等待用户确认完成：**

---

> 最后需要你在多维表格里设置一个自动化，让系统在你上传录音后自动通知我处理。
>
> 1. 打开多维表格：[附上链接]
> 2. 点击右上角「自动化」→「新建自动化」
> 3. **触发条件**：选「记录满足条件时」→ 条件设为「录音文件」「不为空」
> 4. **执行动作**：选「发送飞书消息」→ 接收人选你的工作群
> 5. 消息内容填写：
>    ```
>    录音文件更新提醒：{{会议名称}} 的录音已上传，请查看 {{当前记录链接}}
>    ```
>    然后在消息里 @我（在输入框里 @ 搜索机器人名字）
> 6. 保存并启用
>
> 完成后告诉我，我来验证。

---

⚠️ **为什么这步必须人工做**：飞书多维表格自动化目前没有开放配置 API，只能在 Web UI 里操作。这是整条链路里唯一必须手点的步骤。

---

### 步骤 9：获取工作群 chat_id（agent 自动执行）

```bash
lark-cli chat list
# 找到工作群名称对应的 chat_id（oc_ 开头）
```

---

### 步骤 10：配置 openclaw 群聊（agent 自动执行）

编辑 openclaw.json，添加群聊配置：

```json
{
  "groups": {
    "YOUR_GROUP_CHAT_ID": {
      "requireMention": false
    }
  }
}
```

> ⚠️ 不能用 `gateway config.patch` 修改这个字段，必须直接编辑文件后重启。

---

### 步骤 11：将 SKILL.md 中的占位符全部替换（agent 自动执行）

用上面收集到的值，替换 SKILL.md 配置区的所有 `YOUR_XXX`：

| 占位符 | 来源 | 备注 |
|--------|------|------|
| `YOUR_USER_OPEN_ID` | 步骤 2 | — |
| `YOUR_GROUP_CHAT_ID` | 步骤 9 | — |
| `YOUR_BITABLE_APP_TOKEN` | 步骤 6 | — |
| `YOUR_TABLE_ID` | 步骤 6 | — |
| `YOUR_FORM_VIEW_ID` | 步骤 7 | — |
| `YOUR_FORM_URL` | 步骤 7 | — |
| `YOUR_VOLCENGINE_APP_ID` | 步骤 3 | — |
| `YOUR_VOLCENGINE_TOKEN` | 步骤 3 | — |
| `YOUR_CODEX_PATH` | 步骤 5 | 仅方案 B，否则删除该行 |
| `YOUR_PROXY_PORT` | 步骤 5 | 仅方案 B 且需要代理，否则删除相关两行 |
| `YOUR_OBSIDIAN_PATH` | 询问用户 | 不用 Obsidian 则删除该行 |

同时，在 SKILL.md 阶段四中：
- 方案 A：删除"方案 B"整块内容
- 方案 B：删除"方案 A"整块内容，并将 `YOUR_CODEX_PATH` 和 `YOUR_PROXY_PORT` 替换为实际值

---

### 步骤 12：验证整条链路（agent 自动执行 + 用户配合）

**向用户发送：**

---

> 配置完成！我们来跑一个测试验证一下：
>
> 1. 请私聊发我「记会议纪要」
> 2. 点击卡片填写表单，上传一个很短的录音文件（30秒以内即可）
> 3. 提交后告诉我，我来监控处理过程

---

逐步确认：
- [ ] 私聊卡片正常弹出
- [ ] 工作群收到"录音文件更新提醒"消息并 @了 Bot
- [ ] bitable 状态变为"处理中"
- [ ] ASR 转写完成
- [ ] 纪要生成并回填 bitable 链接
- [ ] bitable 状态变为"已完成"

全部通过 → 配置完成，告知用户可以正式使用。

---

## 组件选择说明

| 组件 | 用途 | 是否必须 |
|------|------|---------|
| openclaw + 飞书机器人 | Bot 收发消息、群聊事件驱动 | ✅ 必须 |
| 飞书多维表格 + 表单 + 自动化 | 录音收集 + 流程触发 | ✅ 必须 |
| 豆包 ASR（火山引擎） | 录音转写 + 说话人分离 | ✅ 必须 |
| Codex CLI | 用独立沙箱生成纪要 | ⬜ 可选，不装则 openclaw 直接生成 |
| Obsidian | 本地备份会议纪要 | ⬜ 可选 |

---

## 多维表格字段结构（参考）

| 字段名 | 字段类型 | 说明 |
|--------|---------|------|
| 创建时间 | 创建时间 | 自动填充 |
| 会议名称 | 文本 | 用户填写 |
| 录音文件 | 附件 | 用户上传 |
| 纪要模板类型 | 单选 | 商务版纪要 / 详细版纪要 / 行动项版纪要 |
| 状态 | 单选 | 处理中 / 失败 / 已完成（空=待处理） |
| 转写文字链接 | 超链接 | Bot 回填 |
| 会议纪要链接 | 超链接 | Bot 回填 |
| 标签 | 文本 | 可选 |

---

## 常见问题

**Bot 收不到群消息**
→ 检查 `requireMention: false` 是否生效（必须直接编辑 openclaw.json 后重启）

**群消息发出了但 Bot 没反应**
→ 检查自动化消息里是否包含"录音文件更新提醒"关键词，且是否 @了 Bot

**ASR 一直轮询 code=2000**
→ 2000 是"正在转写中"不是错误，继续等待，不要停止轮询

**ASR 没有说话人标记**
→ 参数名必须是 `with_speaker_info`，放在 `additions` 里，其他写法被静默忽略

**飞书文档只写入了一小段**
→ 长文本改用 block API 分块写入（5500字/块），见 SKILL.md 阶段五

**Codex hang 住无响应**
→ `pkill -f codex` 后重试，旧 session 未退出会阻塞后续所有调用

**bitable 回填失败 URLFieldPropertyError**
→ 省略 property 参数；值格式必须是 `{"link": "...", "text": "..."}`
